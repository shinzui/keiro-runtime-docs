---
title: "00 — What it demonstrates"
description: "Every keiro runtime feature keiro-runtime-jitsurei exercises, the file(s) that demonstrate each, and the canonical keiro doc for each."
---

The reason to read `keiro-runtime-jitsurei` is that it uses **almost every keiro runtime feature
in anger** — with real domain code, across a real service boundary, with explicit telemetry evidence. If you have
learned a keiro subsystem abstractly elsewhere on this site, this application is where you can see
it wired into a working program.

The application's own feature-selection guide
(`docs/walkthroughs/runtime-feature-use.md`) frames each feature as "reach for X when…"; the table
below turns that into a "demonstrated by" index. Every row links the keiro concept page on the left
to the app source on the right.

## Specification and source ownership

The app is `mori://shinzui/keiro-runtime-jitsurei`. Its permanent project-relative inputs are
`services/incident-command/spec/incident-command.keiro` and
`services/hospital-capacity/spec/hospital-capacity.keiro` (artifact-level URIs pending).
They describe streams, read models, processes, routers, workflows, contracts, inbox/outbox policy,
and Hospital reservation work. This is Keiro DSL 0.3, independently pinned from the newer toolchain
references elsewhere on this site.

Both services use collocated `Generated` modules with checked facts and harnesses. Files bearing
`@generated by keiro-dsl` are overwriteable; Hole modules are create-once and hand-owned. Runtime
transducers/codecs remain hand-owned where list-valued registers, arithmetic updates, composite
router keys, or severity-based timer windows exceed this DSL baseline. Generated types are not
a replacement for those runtime representations.

Run `just dsl-check`, `just dsl-scaffold-preview`, and `just dsl-diff HEAD` from the app root.
The checks cover validation, repeat-scaffold idempotence, the generated import boundary, negative
evolution fixtures, and checked-in output drift. Compile the per-service `*-dsl-test` suites and
run application tests as separate evidence. Preview writes to temporary output; adopt generated
changes without overwriting hand-owned behavior.

The source column below is project-relative beneath each service's `src/` namespace in
`mori://shinzui/keiro-runtime-jitsurei`; artifact-level source URIs are pending.

## Feature → demonstrated by

| Runtime feature | Demonstrated by (app source) | Canonical keiro doc |
|---|---|---|
| Aggregate / transducer + EventStream | `Incident/`, `FieldResource/`, `Triage/`, `Hospital/`, `Capacity/`, `Reservation/` — each a `{Transducer,EventStream}.hs` pair | [event stream &amp; stream](/docs/keiro/reference/event-stream-and-stream) |
| Command cycle (write path) | `*/CommandProcessor.hs` (`runCommandWithProjections`) | [the command cycle](/docs/keiro/explanation/the-command-cycle), [command](/docs/keiro/reference/command) |
| Read models / projections | `*/Projection.hs`, `CommandAudit/Projection.hs` | [projections, read models &amp; snapshots](/docs/keiro/explanation/projections-read-models-and-snapshots), [projection](/docs/keiro/reference/projection) |
| Routers (content-based fan-out) | `ResourceRouter.hs`, `CapacityChangeRouter.hs`, `TransferNeedRouter.hs` | [routers &amp; content-based dispatch](/docs/keiro/explanation/routers-and-content-based-dispatch), [router](/docs/keiro/reference/router) |
| Process managers + durable timers | `Escalation.hs` (`runDueEscalationTimer`), `SurgeManager.hs` | [process managers &amp; sagas](/docs/keiro/explanation/process-managers-and-sagas), [durable timers](/docs/keiro/explanation/durable-timers) |
| Durable workflows + awakeables | `EvacuationWorkflow.hs`, `ReservationWorkflow.hs` | [durable execution](/docs/keiro/explanation/durable-execution), [durable workflows](/docs/keiro/reference/durable-workflows) |
| Outbox / inbox | `*/Integration/{Outbox,Inbox}.hs` | [the outbox pattern](/docs/keiro/explanation/the-outbox-pattern), [the inbox pattern](/docs/keiro/explanation/the-inbox-pattern) |
| Kafka transport (shibuya) | `*/Integration/{KafkaPublisher,KafkaConsumer}.hs` | [integration events](/docs/keiro/explanation/integration-events) |
| Message contracts | `*/Integration/Contracts.hs` | [integration event](/docs/keiro/reference/integration-event) |
| PGMQ background job | `Reservation/WorkQueue.hs`, `Integration/ReservationWorkDispatch.hs` | [pgmq jobs](/docs/keiro/reference/pgmq-jobs), [keiro with pgmq](/docs/integrations/keiro-with-pgmq) |
| TypeID identifiers | `*/Identity.hs`, `*/Domain/Types.hs` | Application TypeID parsing; canonical text in PostgreSQL |
| Telemetry (OpenTelemetry) | `*/Telemetry.hs` | [telemetry](/docs/keiro/reference/telemetry) |
| Wiring / CLI / scenarios | `Store.hs`, `app/*Cli.hs`, `app/*Worker.hs`, `Scenario.hs` | [scaling the workers](/docs/keiro/explanation/scaling-the-workers) |

`*` is `IncidentCommand/` or `HospitalCapacity/` under `services/<service>/src/`. The only feature
that lives in just one service is the **PGMQ background job** — Hospital Capacity's reservation
work queue.

## How the three scenarios exercise the features

The application packages three runnable scenarios. Each lights up a different slice of the runtime
(from `docs/scenarios/README.md`):

| Scenario | Primary boundary | Runtime features it exercises |
|---|---|---|
| `mass-casualty-transfer` | Incident Command → Hospital Capacity | TypeID, Keiki, Kiroku, command processor, process manager, router, workflow, outbox, Kafka, inbox, PGMQ, Shibuya, OpenTelemetry |
| `hospital-divert-reroute` | Hospital Capacity local aggregate + read model | Keiki guard rejection, Kiroku append, capacity read model, reroute-ready state |
| `supply-shortage-escalation` | Hospital Capacity local escalation | Keiki supply register, process manager, timer worker, capacity read model |

`mass-casualty-transfer` is the full cross-boundary demo — it is the one that touches every box in
the architecture diagram, and it is the narrative spine of the
[cross-service](/docs/example-app/cross-service) chapters. The other two are tighter, single-service
stories that isolate guard rejection and local escalation.

## Where each feature is documented in this section

Use the [runtime feature map](/docs/example-app/overview/03-runtime-feature-map) as the index from
each feature to the exact chapter that walks its source. The two service tours
([Incident Command](/docs/example-app/incident-command),
[Hospital Capacity](/docs/example-app/hospital-capacity)) cover the per-service features; the
[cross-service](/docs/example-app/cross-service) tour covers the contracts, outbox/inbox, Kafka, and
trace continuity that join them.
