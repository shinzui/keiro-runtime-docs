---
title: "Running it"
description: "Clone, build, migrate, and run keiro-runtime-jitsurei's three packaged scenarios, then inspect the traces in Jaeger."
---

`keiro-runtime-jitsurei` is meant to be run. This subsection takes you from a clean checkout to a
cross-service trace in Jaeger: the Nix dev shell and local infrastructure
(`process-compose.yaml`), the database and runtime migrations, the three packaged scenarios
(`mass-casualty-transfer`, `hospital-divert-reroute`, `supply-shortage-escalation`), the
per-service workers, and how to read the resulting traces.

<Callout type="warn">
  Scenario `--reset` **drops and recreates the two local development databases** (and recreates the
  Kafka topics). Never point it at a non-development connection string.
</Callout>

<Cards>
  <Card title="00 — Prerequisites and setup" href="/docs/example-app/running-it/00-prerequisites-and-setup" description="The Nix dev shell, local infrastructure, databases, migrations, and building the workspace." />
  <Card title="01 — Running the scenarios" href="/docs/example-app/running-it/01-running-the-scenarios" description="The three scenarios, --trace / --reset, and expected transcripts." />
  <Card title="02 — The workers" href="/docs/example-app/running-it/02-the-workers" description="The per-service worker subcommands and the order the cross-service scenario runs them." />
  <Card title="03 — Observing traces" href="/docs/example-app/running-it/03-observing-traces" description="Open Jaeger, find the cross-service trace, and run the trace verifier." />
</Cards>
