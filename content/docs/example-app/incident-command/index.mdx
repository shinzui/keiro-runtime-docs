---
title: "Incident Command"
description: "A guided source tour of the incident-command service of keiro-runtime-jitsurei."
---

The **`incident-command`** service (Haskell package `incident-command`, modules
`IncidentCommand.*`) is the command-side of the emergency: it declares incidents, assigns
commanders, dispatches field resources, triages casualties, orders evacuations, and escalates
critical incidents on a durable timer. When a triage marks a critical patient it emits a *transfer
need* that travels to Hospital Capacity over Kafka.

This subsection is an ordered source tour, one chapter per concern, that reads the real Haskell at
the pinned commit and links each feature back to its canonical keiro reference page.

<Callout type="info">
  The chapters of this tour land with [Milestone 2](/docs/example-app/overview/03-runtime-feature-map)
  of the documentation plan. Start with the [Overview](/docs/example-app/overview) for the domain
  and the runtime-feature map.
</Callout>
