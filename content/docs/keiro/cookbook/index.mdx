---
title: Cookbook
description: Short, self-contained, copy-paste recipes that each solve one common problem.
---

Short, self-contained, copy-paste recipes that each solve one common problem.

<Cards>
  <Card title="Idempotent fan-out" href="/docs/keiro/cookbook/idempotent-fan-out" description="Dispatch a command from a subscription so at-least-once delivery never double-acts." />
  <Card title="Fan out an event with a router" href="/docs/keiro/cookbook/event-fan-out-with-routers" description="A content-based router that resolves a data-dependent target set from a read model and dispatches one idempotent command to each." />
  <Card title="Timeout saga" href="/docs/keiro/cookbook/timeout-saga" description="Schedule a durable timeout timer and fire a compensating command when it elapses." />
  <Card title="Durable order workflow" href="/docs/keiro/cookbook/durable-order-workflow" description="A complete durable workflow — reserve, sleep, await a webhook, charge, ship via a child — with the resume registry." />
  <Card title="Drain a queue on a schedule" href="/docs/keiro/cookbook/scheduled-job-drain" description="Run a keiro-pgmq job as a one-shot drain on a pg_cron or CronJob cadence with runJobOnce, instead of a long-lived worker." />
  <Card title="Enqueue a job transactionally" href="/docs/keiro/cookbook/transactional-job-enqueue" description="When enqueue's own transaction isn't enough: send onto the queue via raw SQL in the same transaction as a domain write for exactly-once enqueue." />
  <Card title="Enqueue work from a subscription" href="/docs/keiro/cookbook/enqueue-work-from-a-subscription" description="Hand a slow, retryable side effect — an external HTTP call — from a subscription to a keiro-pgmq job, so its retries never block the subscription drain." />
  <Card title="Notify an external system once from a process manager" href="/docs/keiro/cookbook/notify-once-from-a-process-manager" description="When a notification depends on events across several streams, a process manager folds them into one exactly-once decision event, then a job delivers the Slack/email/webhook send." />
  <Card title="Inbox disposition: the three inversions" href="/docs/keiro/cookbook/inbox-disposition-the-three-inversions" description="Write an intake's disposition table the safe way — duplicate is success, previouslyFailed is dead-letter, decodeFailed is dead-letter — and let keiro-dsl check reject the unsafe inversions." />
</Cards>
