---
title: "keiro-ops CLI"
description: "Reference for the keiro-ops operational console: the command tree, global options, the two-phase preview/force contract, schema-drift gating, exit codes, and the AppHooks embedding surface."
---

`keiro-ops` is a standalone database-only console **and** an embeddable command library for operating
a Keiro deployment. *New in Keiro 0.12.0.0.*

Every command is a thin adapter over an exported operation from the library that owns the affected
state. A command never reproduces a supported mutation with ad hoc SQL and never touches another
library's private tables — when an operator primitive is missing, it is added and tested in the owning
library first.

## Invocation

```console
keiro-ops [GLOBAL OPTIONS] <domain> <command> [ARGS]
```

<TypeTable
  type={{
    "--database-url URL": { type: "Maybe Text", default: "environment", description: "PostgreSQL URI or keyword/value string. Falls back to KEIRO_OPS_DATABASE_URL, then DATABASE_URL, then the libpq PG* variables." },
    "--json": { type: "flag", default: "human table", description: "Emit machine-readable JSON. JSON and the human table are alternative renderings of the same structured handler result, so scripts never depend on terminal formatting." },
    "--force": { type: "flag", default: "off", description: "Apply a mutating command after its preview. Without it, a destructive command previews and exits unsuccessfully." },
    "--allow-schema-drift": { type: "flag", default: "off", description: "Allow a mutating command despite a failed schema agreement check. Required in addition to --force." },
  }}
/>

## Safety rails

### Two-phase destructive commands

Every destructive command has two phases, and there is no way to skip the first:

1. **Without `--force`** it performs only the supported read or preview path, renders the rows or
   objects that *would* be affected, prints the exact force-enabled re-invocation, and **exits
   unsuccessfully without mutating anything.**
2. **With `--force`** it calls the supported mutation and reports the actual outcome — including
   idempotent no-ops and already-terminal states.

```console
$ keiro-ops wf cancel orderFulfillment ord-42
… renders what would be cancelled …
preview only; re-run with --force: keiro-ops wf cancel orderFulfillment ord-42 --force
$ echo $?
1
```

<Callout type="info">
  The preview exit status is **1**, deliberately. A preview that has not been applied is not a
  success, so a script that forgets `--force` fails its step rather than silently continuing as if
  the mutation happened.
</Callout>

### Schema verification

Before opening the operational environment, the executable verifies the live Keiro schema with
`Keiro.Migrations.SchemaCheck.verifyExpectedSchema`.

| Command kind | On drift |
|---|---|
| Read-only | continues, after rendering drift warnings |
| Mutating | **refuses** by default; requires `--allow-schema-drift` *in addition to* `--force` |

## Exit codes

| Code | Meaning |
|---|---|
| `0` | success |
| `1` | operational failure, or a preview that was not applied |
| `2` | usage error (unknown command, bad arguments) |

## The command tree

Nine domains are always present and database-only. Two more appear **only** when the embedding binary
mounts the corresponding hook.

### `wf` — durable workflows

| Command | Purpose |
|---|---|
| `list` | list instances with stable keyset paging |
| `show` | show an instance, its children, and its awakeables |
| `steps` | show the derived step index for one generation |
| `journal` | decode one workflow journal generation in order |
| `awakeable show \| signal \| cancel` | inspect, signal, or cancel an awakeable |
| `cancel` | preview or cancel a workflow at its next durable boundary |
| `resurrect` | preview or resurrect a terminally failed workflow |
| `lease release` | preview or forcibly release an instance lease |
| `gc run-once` | preview or run one bounded garbage-collection pass |
| `resume-once` | preview or run one bounded application-registry resume pass *(requires the `workflowResume` hook)* |

`wf resume-once` reports `advanced`, `paced`, and sorted `unregistered_names` in JSON, with
corresponding human columns, so a blocked bounded drain is actionable rather than silent. It also
reports `sleep_due` blockage — a drain that cannot progress because work is waiting on a timer looks
different from one blocked on a foreign lease or an unregistered workflow name.

These wrap the [operator lifecycle APIs](/docs/keiro/reference/durable-workflows#operator-lifecycle-apis)
and inherit the runtime's race and append-only behaviour. In particular, linked-child cancellation
wakes the parent with the typed cancellation result — which is exactly why the CLI delegates instead
of reproducing that transaction.

### `timer` — durable timers

`stuck`, `requeue`, `cancel`, `dead-letter`, `list`, and `drain-once` *(requires the `timerFire`
hook)*.

Keiro 0.14+ outbox output includes `rejected_at`, `rejection_code`, and `rejection_detail`
in text and JSON. `outbox list` accepts `--status rejected`; terminal refusals remain available
for audit and are not removed by `gc-sent`.

### `outbox` — transactional outbox

`backlog`, `list`, `show`, `requeue-stuck`, `gc-sent`, `maintenance-pass`, and `dead-letters list`.

### `inbox` — integration-event inbox

`backlog`, `list`, `show`, `gc`, and `mark-failed`.

### `pgmq` — Keiro PGMQ queues

`dlq`, `read`, `redrive`, `archive`, and `purge`.

### `projection` — projection state

`position` and `prune-dedup`.

`projection position --subscription NAME` preserves durable consumer-group member rows and reports
**both** heads:

| Column | Meaning |
|---|---|
| `store_position` | Kiroku's authoritative append counter — capacity and audit reporting |
| `visible_store_head` | the newest **reachable** event |
| `global_position_distance` | derived from the **visible** head |

<Callout type="warn">
  The distance is computed from the visible head, not the append counter, and is labelled
  **`global_position_distance`** — never event lag, backlog, or "events behind". After a workflow
  garbage collection hard-deletes the newest journal events, the two heads legitimately differ; a
  caught-up projection reports distance **zero** against the visible head and would have reported a
  permanent phantom backlog against the append counter. See
  [the deprecation note](/docs/keiro/reference/telemetry#keiroprojectionlag-is-deprecated).
</Callout>

### `shard` — sharded-subscription ownership

`status` and `relinquish`.

### `snapshot` — advisory snapshots

`show`, `delete`, and `truncation-preflight`.

### `stream` — Kiroku streams

`show`, `soft-delete`, `undelete`, `hard-delete`, `truncate-before set | clear`, `causation`, and
`subscriptions`.

`stream show` takes `--from VERSION` (exclusive cursor, default `0`) and `--limit N` (default `100`).
`truncate-before set` runs a snapshot preflight before offering to write the reversible visibility
marker.

`stream subscriptions` is read-only and database-only. It calls Kiroku's
`subscriptionCheckpointInventory` and preserves every member row: a subscription-wide checkpoint is
the **minimum across matching members**, and member zero does not tell you whether the subscription is
grouped. It reports both the authoritative `store_position` and the reachable `visible_store_head`.

### `replay-audit` — *(hook-gated)*

Audits candidate-code replay against configured targets. Present only when the embedding binary
supplies an `OpsAuditConfig`. See [Replay audit](/docs/keiro/reference/replay-audit).

### `rebuild` — projection catalog *(hook-gated)*

Present only when the embedding binary mounts a
[`ProjectionCatalogOperations`](/docs/keiro/reference/projection-catalog#operator-adapter). Because
that adapter derives everything from one `ValidatedProjectionCatalog`, these commands **cannot** be
given replacement targets, sources, handlers, subscriptions, or dedup lists.

| Command | Phase | Purpose |
|---|---|---|
| `list` | read-only | list the mounted catalog's rebuild groups |
| `preview GROUP` | read-only | preview one catalog group without mutation |
| `start` | preview / `--force` | start one catalog rebuild |
| `status RUN` | read-only | inspect one catalog rebuild run |
| `resume RUN` | preview / `--force` | resume one catalog rebuild run |
| `abandon RUN` | preview / `--force` | abandon one catalog rebuild run |
| `adopt GROUP…` | preview / `--force` | adopt catalog slice changes for the named groups |
| `versioned start \| status \| resume \| abandon` | mixed | operate schema-versioned online rebuilds |
| `retired` | read-only | list retired target generations |
| `drop-retired GENERATION` | preview / `--force` | drop one retired target generation |
| `external-read CONTRACT VERSION` | read-only | inspect one managed external read contract |
| `retire-external-read CONTRACT VERSION` | preview / `--force` | retire one managed external read contract |
| `reproject-stream GROUP PROJECTION STREAM` | preview / `--force` | repair one stream-scoped projection |

#### `rebuild adopt`

Without `--force` it classifies every catalog group and shows the stored and current **slice**
fingerprints. Adoption reconciles registrations transactionally, and its previews are truthful about
what would actually change. This is the sanctioned recovery for a stale group fingerprint after the
`0024` rename of `catalog_fingerprint` to `slice_fingerprint`.

#### `rebuild versioned`

Backed by `ProjectionCatalogOperations` and rendered as versioned JSON envelopes. See
[Versioned projection rebuilds](/docs/keiro/reference/versioned-rebuilds) for the lifecycle these
commands drive. `resume` advances **one durable phase** per invocation rather than looping, so an
operator stays in control of a long convergence.

#### `rebuild drop-retired`

Refuses an active run, a still-compatible external read contract, and ordinary database dependencies.
The preview names the blockers rather than cascading.

#### `rebuild reproject-stream`

Previews exact serving revision, target, dedup, stream-history, and work-admission facts, and
executes only with `--force`.

<Callout type="warn">
  `--max-events` must be **positive** and defaults to **1000**. It is re-checked against locked
  stream metadata *before* the group fence, so a limit that was valid at preview time cannot smuggle
  a larger replay past the fence.
</Callout>

Human and **v2 JSON** outcomes report the admitted limit, cleared rows, replay and application
counts, dedup inserted/existing counts, and verification. Typed refusals carry stable operator codes.

#### `rebuild external-read`

Renders the supported catalog report with contract state, surface generation, PostgreSQL dependents,
and execute grants. Forced retirement goes through the library lifecycle API, never ad hoc `DROP`.

## Embedding

The standalone binary exposes only database-only commands. Commands that need application code are
mounted through explicit hooks — the CLI never invents a database representation of an
application-owned value.

```haskell
data AppHooks = AppHooks
  { workflowResume    :: Maybe ResumeHook
  , timerFire         :: Maybe TimerFire
  , replayAudit       :: Maybe OpsAuditConfig
  , projectionCatalog :: Maybe ProjectionCatalogOperations
  }

emptyAppHooks :: AppHooks

main          :: IO ()
mainWithHooks :: AppHooks -> IO ()

opsCommandTree   :: AppHooks -> ParserInfo OpsInvocation
runOpsInvocation :: AppHooks -> OpsInvocation -> IO ExitCode
```

An embedding binary is usually four lines:

```haskell
module Main (main) where

import Keiro.Ops qualified as Ops

main :: IO ()
main = Ops.mainWithHooks Ops.emptyAppHooks { Ops.projectionCatalog = Just myCatalogOps }
```

`opsCommandTree` and `runOpsInvocation` are exposed separately so a future TUI or web console can
reuse the handlers. Reusing them does **not** grant permission to bypass the library boundary: a
console still calls the owning library's exported operations.

<Cards>
  <Card title="Operate a deployment with keiro-ops" href="/docs/keiro/how-to/operate-a-deployment-with-keiro-ops" />
  <Card title="Durable workflows" href="/docs/keiro/reference/durable-workflows" />
  <Card title="Projection catalog" href="/docs/keiro/reference/projection-catalog" />
  <Card title="Versioned rebuilds and external reads" href="/docs/keiro/reference/versioned-rebuilds" />
  <Card title="Migrations and schema" href="/docs/keiro/reference/migrations-and-schema" />
</Cards>
