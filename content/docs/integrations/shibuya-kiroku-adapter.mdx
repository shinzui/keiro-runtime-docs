---
title: shibuya ⇄ kiroku adapter
description: The headline cross-package adapter — feeding kiroku events through shibuya pipelines.
---

This is the headline cross-package integration: an adapter that feeds events from
kiroku (記録, an append-only PostgreSQL event store) through shibuya's supervised,
Broadway-style queue pipelines for concurrent processing with backpressure.

<Callout type="info">
  Documentation in progress. This page will describe the adapter's wiring, delivery guarantees, and
  a runnable jitsurei (実例, worked example).
</Callout>
