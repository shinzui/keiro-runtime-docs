.claude/
.agents/
.seihou/manifest.json.tmp
node_modules
dist
build
.direnv
result
result-*
*.log
.env
.env.local
bun.lock.bak
.DS_Store
.pre-commit-config.yaml
CLAUDE.local.md

# Vite / TanStack Start / Nitro / fumadocs (generated)
.output
.nitro
.tanstack
.cache
.vercel
.source
out
pnpm-debug.log*
tsconfig.tsbuildinfo

# Note: src/routeTree.gen.ts is generated by the TanStack Router plugin but is
# committed (so `tsc --noEmit` works without a prior dev/build). Do not ignore it.
