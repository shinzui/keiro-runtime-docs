# pnpm 11 reads settings from this file (the package.json "pnpm" field is no
# longer honored). Allow the native build scripts these deps need:
#   - esbuild: installs its platform binary (used by fumadocs-mdx)
#   - sharp:   Next.js image optimization native module
allowBuilds:
  esbuild: true
  sharp: true
